{
  "name": "gcpusage",
  "version": "1.0.1",
  "description": "GLM Coding Plan usage monitor CLI tool",
  "type": "module",
  "bin": {
    "gcpusage": "./dist/cli.js"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "start:node": "tsx src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target node && bun scripts/build.js && chmod +x dist/cli.js",
    "dev": "bun --watch src/cli.ts"
  },
  "keywords": ["glm", "usage", "monitor", "cli"],
  "license": "MIT",
  "files": ["dist"],
  "dependencies": {
    "cli-table3": "^0.6.5"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3",
    "tsx": "^4.19.2"
  }
}
